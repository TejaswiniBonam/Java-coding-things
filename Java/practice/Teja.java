/**
 * Java file and class naming rules:
 * <ul>
 *   <li>The filename must exactly match the public class name defined within the file, including case sensitivity.</li>
 *   <li>Filenames must end with the <code>.java</code> extension.</li>
 *   <li>Class names should start with an uppercase letter and follow CamelCase convention (e.g., <code>MyClassName</code>).</li>
 *   <li>Only one public class is allowed per file, and its name must match the filename.</li>
 *   <li>Other (non-public) classes can be defined in the same file, but their names do not need to match the filename.</li>
 *   <li>Class names should not contain spaces or special characters (only letters, digits, and underscores are allowed).</li>
 *   <li>Class names should not start with a digit.</li>
 * </ul>
 */
// <file_name>.java 
// file_name - Harshi , Harshi.java
// HarshitaKagitha - camel case
// java_file -> class -> Methods


public class Teja{ // defining class
    // methods, constructors.. 
    /**
     * Rules for the main method in Java:
     * <ul>
     *   <li>The main method must be <code>public</code> so that it is accessible to the JVM.</li>
     *   <li>The main method must be <code>static</code> so that it can be called without creating an instance of the class.</li>
     *   <li>The return type must be <code>void</code> because it does not return any value.</li>
     *   <li>The parameter must be a single array of <code>String</code> objects (<code>String[] args</code>), which stores command-line arguments.</li>
     *   <li>The correct signature is: <code>public static void main(String[] args)</code></li>
     * </ul>
     */
    // main method - IMPORTANT
    public method1(){

    }
    public method2(){
        
    }
    public static void main(String args[]){

        // body 
        System.out.println("Hello World"); // print statement with new line 
        System.out.print("Hello World"); // print statement without new line
    }

}
